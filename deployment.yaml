apiVersion: apps/v1
kind: Deployment
metadata:
  name: fedrelax-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fedrelax
  template:
    metadata:
      labels:
        app: fedrelax
    spec:
      containers:
      - name: fedrelax-container
        image: quynhhgoogoo/fed-relax
        ports:
        - containerPort: 80